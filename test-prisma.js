import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { try { await prisma.category.findMany({ where: { isActive: true }, include: { parent: true, children: true }, orderBy: { name: "asc" } }); console.log("Success findMany"); } catch (e) { console.error("Error Message:", e.message); } finally { await prisma.$disconnect(); } } main();
